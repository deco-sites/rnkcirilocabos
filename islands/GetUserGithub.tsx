import Component from "$store/components/GetUserGithub.tsx";
import type { Props } from "$store/components/GetUserGithub.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
